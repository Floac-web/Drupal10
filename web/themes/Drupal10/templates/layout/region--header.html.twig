
{%
  set classes = [
    'header',
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <header{{ attributes.addClass(classes) }}>
    <div class="container">
      <div class="header-wrap">
        {{ content }}
      </div>
    </div>
  </header>
{% endif %}
